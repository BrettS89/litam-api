const getMyAlarms = require('./getAlarms');
const getAlarmMessage = require('./getAlarmMessage');
const stopAlarm = require('./stopAlarm');

module.exports = {
  getMyAlarms,
  getAlarmMessage,
  stopAlarm,
};
